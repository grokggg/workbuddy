# HANDBOOK.md — 目标情报工具链工程手册

> M22: 从教科书到工程手册。每个模块从第一性原理推导, 记录设计权衡与失败根因。

## 一、第一性原理

### 1.1 为什么需要"探测"?
**第一性原理**: 你无法处理一个你不知道是什么的目标。
- 不知道文件格式 -> 无法解析结构
- 不知道架构 -> 无法反汇编
- 不知道是否加壳 -> 静态分析全错
- 不知道保护机制 -> 对策全错

**推导**: 探测 = 把"未知目标"映射到"已知处理路径"的分类器。
所以探测模块的成败标准不是"识别多少特征", 而是**能否给出正确的处理路径建议**。

### 1.2 为什么需要"分析"?
**第一性原理**: 修改一个二进制 = 在"语义正确"的前提下改写字节。
- 语义来自指令序列(静态)
- 行为来自运行观察(动态)
- 两者结合才构成"理解"

**推导**: 分析 = 指令模式识别 + API 引用 + 行为签名三者的交叉验证。
单靠指令模式会误报(数据段里也有字节序列), 单靠 API 引用会漏(内联实现),
单靠行为观察无法覆盖全路径。**三个信号交叉才是工程级的做法**。

### 1.3 为什么需要"应对"?
**第一性原理**: 保护机制的本质是"增加篡改成本"。应对的本质是"在某个层面把成本降到可接受"。
- 加壳 = 混淆静态结构 -> 应对: 运行时还原
- 反调试 = 检测观察者 -> 应对: 消除观察痕迹
- 反VM = 检测环境 -> 应对: 伪造环境
- 完整性 = 检测篡改 -> 应对: 保持原文件 + 运行时 hook
- 反重打包 = 检测签名 -> 应对: 重签或 patch 校验

**推导**: 应对不是"破解", 是对"保护机制防御面"的系统评估。
每个应对思路都要给**可行性评估**——这决定了资源分配。

## 二、设计决策权衡

### 2.1 熵检测: 全局 vs 分段
| 方案 | 优点 | 缺点 | 决策 |
|---|---|---|---|
| 全局熵 | 简单 | 数据段稀释代码段(实测 busybox 熵 6.027 误判正常) | 弃 |
| **分段熵** | 区分代码段/数据段 | 需定 chunk 大小 | **采**(v2) |

**失败案例 F1**: busybox 全局熵 6.027, 单阈值 6.5 判正常。但 busybox 是静态编译,
代码段熵应更高。根因: 全局熵被大量字符串数据稀释。修复: 分段熵 + 高熵块比例判定。

### 2.2 文件类型判定: 入口点 vs 类型字段
| 方案 | 优点 | 缺点 | 决策 |
|---|---|---|---|
| 只看入口点 | 简单 | 共享库入口点 0x0 误判 | 弃 |
| **类型字段**(ELF e_type / PE 子系统) | 准确 | 需解析结构 | **采**(v2) |

**失败案例 F2**: libsqlite3.so 入口点 0x0, v1 报"入口点 0x0"而非"共享库"。
根因: 只看入口点不看 e_type。修复: 解析 ELF e_type(ET_DYN=共享库)。

### 2.3 校验识别: 正则 vs 指令解码
| 方案 | 优点 | 缺点 | 决策 |
|---|---|---|---|
| 正则匹配字节 | 快 | 数据段误报 | 混合 |
| 完整指令解码 | 准 | 慢, 需完整 disasm | 混合 |

**失败案例 F3**: v1 的 cmp+jcc 正则对真实二进制(openssl/sqlite)识别率低。
根因: 真实代码的校验不总是"cmp imm + jcc"连续, 中间有 mov/lea。
修复: 模式库扩展 + API 引用交叉验证(v2 在 sqlite 上检出 111 处)。

### 2.4 动态调试: ptrace 真实 vs mock
| 方案 | 优点 | 缺点 | 决策 |
|---|---|---|---|
| ptrace 真实 | 真实 | 需权限, 复杂 | 条件采 |
| mock | 可测 | 非真实 | 退化 |

**决策**: 有 ptrace 用真实现, 无权限退 mock(明确标注)。工程手册原则:
**永远有可运行的退化路径**, 而不是失败报错。

## 三、失败案例根因分析

### 案例 1: busybox 加壳误判
- 现象: 熵 6.027 未标可疑
- 根因: 全局熵被稀释
- 修复: 分段熵
- 教训: **单指标判定不可靠, 要多信号交叉**

### 案例 2: sqlite 共享库识别错误
- 现象: 入口点报 0x0
- 根因: 只看入口点
- 修复: 解析 e_type
- 教训: **结构字段比派生特征可靠**

### 案例 3: openssl 下载不完整
- 现象: tar 解包报 Unexpected EOF
- 根因: 网络截断(curl 没加 -f)
- 修复: 用系统已有二进制(libsqlite3.so)替代
- 教训: **真实目标获取要有多源备选, 不要单点依赖**

### 案例 4: sqlite.c 下载是坏 zip
- 现象: BadZipFile
- 根因: 下载不完整 + 文件名误导(.c 实际是 zip)
- 修复: 检查魔数而不是扩展名
- 教训: **永远按魔数判断文件类型, 不信扩展名**

## 四、原创设计

### 4.1 五类保护机制统一识别框架
**自己定义的分类法**: 加壳/反调试/反VM/完整性/反重打包
- 每类 = 特征集 + 检测方法 + 应对思路 + 可行性
- 统一由 recon_v2 输出, countermeasure 消费
- 这是"检测-应对闭环"而不是孤立脚本

### 4.2 行为建模(离线静态近似)
**原创**: 用静态特征(API 引用组合)推断运行时行为
- IsDebuggerPresent + NtQueryInformationProcess -> anti-debug(0.8)
- VirtualProtect + WriteProcessMemory -> self-modifying(0.7)
- GetTickCount + QPC -> timing-check(0.6)
- 置信度分级 = 对推断可靠性的显式标注

### 4.3 应对可行性分级
**原创**: 每个应对思路带 feasibility(高/中/低)
- 决策依据: 高可行性先做, 低可行性评估资源
- 这不是教科书的"怎么破", 是工程手册的"该不该试"

## 五、工程手册原则(从失败中提炼)

1. **多信号交叉**: 单指标必然误判(busybox 熵)
2. **结构优先**: 解析结构字段, 不信派生特征(sqlite e_type)
3. **退化路径**: 每个功能有可运行的 mock 退化(dynamic_debug)
4. **多源备选**: 真实目标获取不单点依赖(openssl 下载失败)
5. **魔数优先**: 判断文件类型用魔数不用扩展名(sqlite.c)
6. **失败即数据**: 每个失败记录根因, 成为下一版的设计输入

## 六、M23 新增失败案例(M23: 真实目标库扩展)

### 案例 5: asar 格式未识别(F9)
- 现象: electron-app.asar 报 Unknown
- 根因: recon_v2 只认 PE/ELF/Mach-O/Script, 无 asar
- 修复: 新增 `_is_asar`(pickle 头 + header size 范围 + json 含 "files"),
  支持两种头布局(标准/对齐)
- 教训: **格式识别要覆盖真实目标生态(Electron/asar 是主流打包格式)**

### 案例 6: MPRESS 误报(F10)
- 现象: curl-real(Linux ELF)检出 MPRESS(Windows 壳)
- 根因: 字符串巧合(curl 代码含 "MPRESS" 字面量)
- 修复方向: 壳标记需与文件格式平台匹配(Windows 壳只在 PE 里算特征)
- 教训: **特征必须带平台上下文, 跨平台字面量会误报**

### 案例 7: openssl 无二进制(F7)
- 现象: 下载的是源码 tar.gz, 无编译产物
- 根因: 无 gcc 环境 + 官方不发布静态二进制
- 修复方向: 换编译环境 / 找第三方静态构建
- 教训: **真实目标获取要评估"能否得到可执行形式", 源码不等于目标**

### 案例 8: 完整性字符串分类
- 现象: busybox(功能引用)与 asar(自校验)都检出完整性字符串
- 根因: 字符串本身无区分度, 需上下文
- 发现: 自校验有 GetModuleFileName+ReadFile 自读模式
- 教训: **特征要分级: 字符串=弱信号, 自读模式=强信号**

## 七、M23 原创设计

### 7.1 asar 格式识别器
- 两种头布局兼容
- 大小范围校验防误报(0x10-0x100000)
- json 内容确认("files" 键)

### 7.2 平台上下文过滤(设计)
- 壳标记库标注平台(UPX=跨, MPRESS=Win, VMProtect=Win)
- 只在与平台匹配的文件格式上算特征
- 降低跨平台字面量误报

## 八、M24 新增失败案例(M24: 硬伤修复 + 真实商业目标)

### 案例 9: MPRESS 误报(F11)
- 现象: curl(Linux ELF)检出 MPRESS(Windows 壳)
- 根因: 字符串匹配无平台过滤
- 修复: PACKER_MARKERS 加 platforms 字段, detect_packer 按格式过滤
- 测试: test_platform_mismatch_fp(ELF+MPRESS 不命中)
- 教训: **壳特征必须带平台上下文**

### 案例 10: VMP 子串误报(F12)
- 现象: selenium-manager(Go 编译)检出 VMProtect
- 根因: "VMP" 3 字符特征匹配到 "VMPike" 字符串巧合
- 修复: 移除 VMP, 保留 VMProtect/VMProtectSDK
- 教训: **特征长度 ≥6 字符, 短子串(<5)不可单独判壳**

### 案例 11: 真实加壳目标
- 用 UPX 4.0.1 对真实 openssl 加壳: 1.1MB → 370KB(33.5%)
- 加壳: 熵 2.7→7.19, UPX 检出
- 脱壳(upx -d): 熵回 2.7, 无壳 ← 应对闭环验证
- 教训: **自建加壳样本用真实工具+真实二进制, 不是手写特征**

### 案例 12: Go 编译二进制
- selenium-manager: 反调试 API 是真实导入(强信号)
- 但数据区大量字符串巧合(弱信号)
- 教训: **导入表 API = 强信号, 数据区字符串 = 弱信号, 要分级**

## 九、M24 原创设计: 特征分级

| 信号强度 | 特征类型 | 示例 |
|---|---|---|
| 强 | 导入表 API | IsDebuggerPresent 在 IAT |
| 强 | 段名匹配 | UPX0/UPX1/.vmp0 |
| 中 | 长字符串(≥6字符) | VMProtect/MPRESS |
| 弱 | 短子串(<5字符) | VMP/FSG! |
| 弱 | 数据区字符串 | md5/sha(功能引用) |

**规则**: 强信号可单独判, 弱信号需多信号交叉 + 平台过滤。
